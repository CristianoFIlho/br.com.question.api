# Salesforce Quiz API - Environment Configuration

# Database Configuration
DATABASE_URL=sqlite:///./test.db
# For PostgreSQL in production:
# DATABASE_URL=postgresql://username:password@localhost:5432/salesforce_quiz_db

# Security Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production-minimum-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Salesforce Quiz API
PROJECT_VERSION=1.0.0

# CORS Configuration (comma-separated origins)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://localhost:3000,https://localhost:3001

# Environment
ENVIRONMENT=development
# Options: development, staging, production

# Optional: Email Configuration (for future features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_TLS=true

# Optional: Redis Configuration (for caching)
# REDIS_URL=redis://localhost:6379/0

# Optional: Logging Configuration
# LOG_LEVEL=INFO
# LOG_FILE=logs/app.log
